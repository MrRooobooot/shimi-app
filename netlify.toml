# Netlify config — static site, no build step
[build]
  publish = "."
  command = ""

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "public, max-age=300"
    X-Frame-Options = "ALLOWALL"
